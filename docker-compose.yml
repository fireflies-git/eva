services:
  eva:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - STATE_DIR=/app/state
    volumes:
      - ./.env:/app/.env:ro
      - ./yuri.db:/app/state/yuri.db:ro
      - eva-state:/app/state

volumes:
  eva-state:
